### Project 3 — SQL Data Analysis
Imported the dataset into Microsoft SQL Server and wrote 12 queries covering SELECT, WHERE, GROUP BY, ORDER BY, COUNT, SUM and AVG.

## Tools Used
SQL Server Management Studio | Microsoft Word | Microsoft Excel
